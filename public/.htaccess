<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # 我們暫時忽略您舊的、複雜的網域對應規則
    # 這裡只需要一條規則：將所有不存在的檔案/目錄請求，都交給 index.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php [L,QSA]
</IfModule>